# Dev-only stress harness — must never reach the Pages deploy.
# Content is preserved in git history (commit c8eed2b) if you need it back.
js/test.js

# Marketing assets — local only, never deploy.
marketing/
